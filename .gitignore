# Environment & secrets
.env

# Local database (each env has its own)
admin/database/*.sqlite*

# Dependencies
node_modules/

# Explicit .env protection for all subdirs
server/.env
admin/.env
*.env
*.env.*
!*.env.example
